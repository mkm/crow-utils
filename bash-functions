j() {
    dir=$(finddir $@)
    ret=$?
    if [ "$ret" -eq "1" ]
    then
        echo "No such directory."
        return 1
    fi

    if [ "$ret" -eq "2" ]
    then
        echo "$dir"
        return 2
    fi

    cd $dir
}